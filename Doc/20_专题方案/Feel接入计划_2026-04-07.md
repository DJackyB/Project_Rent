# Feel v5.9.1 安装与首轮接入计划

## 摘要

- 目标包：[Feel v5.9.1](E:/SDL/Feel/Feel%20v5.9.1%20(08%20Dec%202025).unitypackage)。
- 安装策略：导入 Feel 核心资源，删除 demo / sample / test scene 等非运行时必要内容；首轮只做视觉反馈增强。
- 音频边界：音频继续由 `Martian.Audio`、`AudioEventBridge`、`AudioCatalog` 作为唯一真源，不用 Feel 替换音频系统。
- 反馈边界：Feel 不替换现有 `Martian.Feedback`，只作为可选视觉后端接在现有反馈链路下。
- 模块状态：**已导入，未接线**。当前只完成包体整理；运行时接入仍按本文后续方案执行。

## 运行时链路

```
BaoZuPoFeedbackAdapter（不变，继续翻译业务语义）
  │
  └─▶ FeedbackServiceLocator.Current.Publish / PublishSequence
        │
        ▼
  FeedbackPlaybackCoordinator（不变）
        │
        ▼  .SetBackend(composite)
  CompositeFeedbackPlaybackBackend（新增）
        ├── 主：FloatingTextFeedbackBackend  ← 控制时序，返回 handle
        └── 副：FeelFeedbackBackend          ← 视觉增强，handle 丢弃

接线触发：
  BaoZuPoFeelFeedbackInstaller（新增 MonoBehaviour）
    ├── [SerializeField] FeedbackBootstrap _bootstrap
    ├── [SerializeField] MMF_Player _moneyDeltaPlayer
    ├── [SerializeField] MMF_Player _settlementStepPlayer
    ├── [SerializeField] MMF_Player _loanPaymentPlayer
    ├── [SerializeField] MMF_Player _cardPlayPlayer
    └── [SerializeField] MMF_Player _rewardRevealPlayer
```

## 安装步骤

1. 在 Unity 中导入：`AssetDatabase.ImportPackage("E:/SDL/Feel/Feel v5.9.1 (08 Dec 2025).unitypackage", false)`。
2. 删除非必要样例内容：`FeelDemos`、`FeelDemosHDRP`、`FeelDemosURP`、`MMFeedbacks/Demos`、`MMTools/Demos`、`NiceVibrations/Demo`、`NiceVibrations/HapticSamples`、`NiceVibrations/OlderVersions`，以及散落的 demo/test 文件。
3. 保留 `MMFeedbacks`、`MMTools`、`NiceVibrations` 核心目录和 license/readme，避免破坏 Feel 自身依赖关系。
4. 导入后检查项：
   - Console 无项目侧编译错误。
   - `Assets/Feel/...` 已导入。
   - 如果 Feel optional integrations 引发编译错误，停止接入并报告，不直接改 vendor 代码。
   - **过滤 Console 中的 `MMSoundManager` 关键字**，确认它没有在场景启动时自动 Awake 或产生任何 Log。如有，记录具体行为，在继续之前决策是否需要条件编译隔离。
   - Package Manager 不新增第二套项目音频真源。

## 新增代码

所有新增代码放在 `Assets/_Assets/Scripts/Integration/Feel/`，不进入 `Martian/Feedback/` 目录。

### CompositeFeedbackPlaybackBackend

实现 `IFeedbackPlaybackBackend`。持有一个主后端和一个副后端。

核心行为：
- `Publish` / `PublishSequence`：先调主后端拿到 handle，再调副后端（丢弃其 handle）；只返回主后端 handle。
- `IsAvailable`：取主后端的 IsAvailable；副后端不可用时不影响主后端判断。
- `AllPlaybackCompleted`：只订阅主后端的事件并转发。
- `Configure`：转发给两个后端。
- `Attach`：转发给两个后端。
- `Clear`：先 Clear 副后端，再 Clear 主后端。副后端 Clear 抛异常时 catch + LogError，不阻断主后端。

### FeelFeedbackBackend

实现 `IFeedbackPlaybackBackend`。把 `FeedbackRequest` / `FeedbackSequenceRequest` 映射到配置好的 `MMF_Player` 视觉预制体。

映射规则：
| FeedbackCategory | 对应 MMF_Player 字段 | 是否必填 |
|---|---|---|
| Money（NumericDelta > 0）| `_moneyDeltaPlayer` | 可选 |
| Cost（NumericDelta < 0）| `_moneyDeltaPlayer` | 可选 |
| Settlement | `_settlementStepPlayer` | 可选 |
| Loan | `_loanPaymentPlayer` | 可选 |

另有两个不走 FeedbackCategory 的预制体：
- `_cardPlayPlayer`：由 UI 层直接调用（`UICardDragController` 出牌成功时）
- `_rewardRevealPlayer`：由 UI 层直接调用（`UICardRewardPanel` 出现/选择时）

`IsAvailable` 规则：
- 至少有一个 `MMF_Player` 引用非 null → `true`。
- 全部为 null → `false`。

单条请求找不到对应 `MMF_Player` 时：`Debug.LogWarning` 并跳过该条，不阻断主后端，不返回有效 handle。

`Publish` / `PublishSequence` 返回的 handle 始终立即 Complete（Feel 动画不参与时序控制）。

### BaoZuPoFeelFeedbackInstaller

独立 MonoBehaviour，**不挂在** `FeedbackBootstrap` 所在的 GameObject 上（`FeedbackBootstrap` 标记了 `[DisallowMultipleComponent]`）。推荐挂在同一 Canvas 下的独立子节点。

字段：
- `[SerializeField] private FeedbackBootstrap _bootstrap;`
- 5 个 `[SerializeField] private MMF_Player` 字段（对应上面的映射表）

生命周期：
- `Awake`：检查 `_bootstrap == null`，如果为 null 则 `Debug.LogError` 并 `enabled = false`。检查所有 `MMF_Player` 引用，缺失任何一个时 `Debug.LogWarning`（单个缺失不阻断启动）。
- `Start`：构造 `FeelFeedbackBackend`（传入 MMF_Player 引用），构造 `CompositeFeedbackPlaybackBackend`（主 = null 让 Coordinator 自己的 FloatingText 作为主，副 = FeelFeedbackBackend），调用 `_bootstrap.SetBackend(composite)`。
- `OnDestroy`：调用 `_bootstrap.SetBackend(null)`，让 Coordinator 恢复默认 `FloatingTextFeedbackBackend`（见 `FeedbackPlaybackCoordinator.SetBackend` 源码第 69 行：`_backend = backend ?? new FloatingTextFeedbackBackend()`）。

## 首轮效果预制体

放在 `Assets/_Assets/Prefabs/Feedback/Feel/`：

| 预制体 | 场景 | 表现 |
|--------|------|------|
| `MoneyDeltaFeedback` | 出牌费用、即时收益、结算总金额 | 金币增减飘字 + 轻微弹动 |
| `SettlementStepFeedback` | 结算时房间/合约锚点 | 轻量 pulse / 高亮，不改变结算顺序 |
| `LoanPaymentFeedback` | 每 5 回合还贷 | 红色 UI 闪烁 / 金额强调，不用 Camera Shake |
| `CardPlayFeedback` | 出牌成功 | 轻微缩放 / 光效；实际移动仍由 DOTween 拖拽逻辑负责 |
| `RewardRevealFeedback` | 奖励三选一 | 淡入 / 选中反馈 |

所有 `MMF_Player` 中的 Audio / Sound feedback **首轮禁用或不配置**。

## 接线位置

- 结算、费用、即时金钱、贷款 → 继续走 `BaoZuPoFeedbackAdapter`，不改动。Composite 后端透明拦截。
- 卡牌拖拽 → `UICardDragController`，出牌成功时直接调用 `FeelFeedbackBackend.PlayCardPlay()`。
- 奖励面板 → `UICardRewardPanel`，通过 Installer 暴露的公共方法调用。
- 玩法核心脚本 **不直接依赖** `MoreMountains.*`。所有 Feel 调用收口在 `Integration/Feel/` 和 UI 层。

## 降级策略

与项目原则对齐：**配置错误报错中断，功能缺失可降级。**

| 情况 | 行为 |
|------|------|
| `BaoZuPoFeelFeedbackInstaller` 未挂 / 场景中不存在 | 静默降级，FloatingText 照常工作，游戏不受影响 |
| Installer 存在但 `_bootstrap` 引用为 null | `Debug.LogError` + 自我禁用，不继续接线 |
| Installer 存在、bootstrap 有效，但某个 MMF_Player 为 null | `Debug.LogWarning`（Awake），对应类别跳过 Feel 增强，其余正常 |
| Feel 后端运行时 `MMF_Player.PlayFeedbacks()` 抛异常 | Composite 的 catch 吞掉异常并 `Debug.LogError`，主后端不受影响 |

## 验证计划

### 安装后

- Console 无项目侧 error。
- `Assets/Feel/...` 已导入。
- Package Manager 不新增第二套音频真源。
- 过滤 `MMSoundManager`：未自动初始化、无 DontDestroyOnLoad 残留。

### 自动化

- 跑现有 EditMode 测试，当前基线 `69/69 passed`。
- 新增测试覆盖：
  - `CompositeFeedbackPlaybackBackend`：返回主后端 handle，副后端不可用时不影响主反馈。
  - `FeelFeedbackBackend`：缺 MMF_Player 时 IsAvailable 行为正确、单条请求跳过不阻断。
  - `BaoZuPoFeelFeedbackInstaller`：bootstrap 引用为 null 时自我禁用。

### 手工 Play Mode

- 开局进入 Action 阶段，手牌正常生成。
- 拖拽卡牌：拖起、合法目标、非法返回、出牌成功都有视觉反馈，且卡牌实际出牌逻辑不变。
- 结算阶段：房间/合约结算仍逐步播放，顶部金钱延迟显示仍正确，阶段不会因为 Feel 提前或延迟而卡住。
- 贷款回合：扣款反馈出现，失败/破产流程仍正常。
- 奖励三选一：面板出现、选择、跳过都能继续流程。
- 音量设置仍控制所有实际音效；Feel 不绕过 `Martian.Audio` 播放声音。
- 禁用 Installer 组件后，游戏完全不受影响，仅失去 Feel 视觉增强。

## 当前导入状态

- 已保留：`Assets/Feel/MMFeedbacks`、`Assets/Feel/MMTools`、`Assets/Feel/NiceVibrations`、`Assets/Feel/license.txt`、`Assets/Feel/readme.txt`。
- 已删除：`FeelDemos`、`FeelDemosHDRP`、`FeelDemosURP`、`MMFeedbacks/Demos`、`MMTools/Demos`、`NiceVibrations/Demo`、`NiceVibrations/HapticSamples`、`NiceVibrations/OlderVersions`、`MMProgressBarDemoAuto.cs`、`MMSaveLoadTestScene.unity`。
- 暂未删除：`MMTools/Core/MMSceneLoading/LoadingScreens`。这些更像工具模板而不是 demo，当前先保留，后续确认不使用 `MMSceneLoading` 后再整体裁剪。
- 暂未接线：没有新增 `CompositeFeedbackPlaybackBackend`、`FeelFeedbackBackend`、`BaoZuPoFeelFeedbackInstaller`，也没有在场景或 UI 中引用 `MMF_Player`。

## 假设与默认选择

- 导入后只删除明确的 demo / sample / test scene 内容；不拆分 Feel 核心运行时目录，先保证 vendor 包可编译。
- Feel 是可选表现层：未启用时游戏仍可玩；一旦启用但必要引用缺失，按上方降级策略处理。
- 首轮不接 URP Volume、Cinemachine、MMSoundManager、NiceVibrations；只做 Canvas/UI 级视觉反馈，避免影响发售前主链路稳定性。

## 后续记录区

首轮接入完成后，在此记录实际遇到的问题和决策变更。
