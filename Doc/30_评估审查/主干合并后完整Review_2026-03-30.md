# 主干合并后完整Review（2026-03-30）

> 审查范围：当前 `main` 主干代码与 `Doc/` 维护入口  
> 审查重点：近期合并后的奖励选择、牌库配置、模块边界、文案真源
> 验证状态：未运行自动化测试，以下结论基于代码审查与本轮修订后的复核

## 已处理项

- 奖励 UI 缺失不再静默卡死，已改成主链路必需引用，缺失时直接报错暴露。
- `boosted` 奖励在 Rare 池为空时不再整段丢失，已改成 warning + 回退普通奖励池。
- `GameManager` 对 `MoneyManager / BoardManager / DeckManager` 已改成启动期 fail-fast。
- 奖励相关 EditMode 测试已改成事件驱动语义，并补了 boosted 回退与初始化缺失校验。
- 奖励候选已改成同一轮内不重复；唯一候选不足 3 张时会 warning 并只展示可用的唯一候选。
- 旧运行时本地化链路已删除，当前固定文案真源已收口到 `GameText`。
- 代码中的中文字符串字面量已统一改成英文，旧中文字体资源也已移除。

## Findings

### P2：仓库里仍混入了本机本地配置文件

- `.claude/settings.local.json` 里包含本机权限白名单和绝对路径。
- `.idea/.idea.Project_Rent/.idea/vcs.xml` 这类 IDE 元数据只反映个人机器状态，不是工程真源。

建议：

- 这类文件从主干移出，并补 `.gitignore` 或团队约定，避免继续制造无意义 diff 与路径泄漏。

### P3：多语言模块仍是“已入库、未接线”状态

- 仓库中保留了 `Martian.Localization` 与 `com.unity.localization`。
- 当前主干运行时并没有接这套模块，项目实际真源是 `GameText`。
- 这不是 bug，但如果后续要重新做多语言，必须明确一次正式接入，不适合长期模糊并存。

建议：

- 后续若重做多语言，直接以 `GameText` 为调用面做一次正式切换，不要恢复旧双语分支。

## Review 结论

- 当前主干最危险的奖励链路问题已经收掉：缺 UI 会直接暴露，boosted 池为空会 warning 并回退。
- 奖励面板不会再出现重复候选，但奖励池唯一候选不足 3 张时仍会退化成少于 3 个选项。
- Save / Localization 模块边界现在是清楚的：Save 仅保留模块本体，本地化当前只有英文单真源在跑。
- 当前最值得继续处理的剩余问题，是仓库本机配置清理，以及未来多语言接入时的一次正式模块化切换。
