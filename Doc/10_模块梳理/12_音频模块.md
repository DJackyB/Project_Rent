# 12_音频模块

## 状态：已接线（代码完成，资产待配置）

---

## 架构

```
游戏逻辑 → EventBus → AudioEventBridge → AudioServices.Current → UnityAudioBackend
                                                                 ↳ 未来可替换为 WwiseBackend
通用 Button → AudioOnClick 组件 → AudioServices.Current
```

底层使用 `Packages/com.martian.audio`（UPM 本地包，版本 0.1.0）。
项目侧只有两个新文件，不修改包内代码。

---

## 文件清单

| 文件 | 说明 |
|---|---|
| `Packages/com.martian.audio/` | 音频包（已存在，不改动） |
| `Assets/Scripts/Runtime/Integration/Martian/Audio/AudioEventBridge.cs` | EventBus 事件 → 音频调用映射 |
| `Assets/Scripts/Runtime/UI/AudioOnClick.cs` | 挂在 Button 上的通用点击音组件 |
| `Assets/Scripts/Runtime/Integration/Martian/Audio/AudioVolumeController.cs` | 四路音量控制，挂在 [Audio] 节点，设置面板 Slider 绑定入口 |
| `Assets/Data/Audio/AudioCatalog.asset` | 音效 Cue 定义（**待创建**） |
| `Assets/Data/Audio/AudioSettings.asset` | 音频系统配置（**待创建**） |

---

## 接线步骤（手工）

1. 在 `Assets/Data/Audio/` 创建 `AudioCatalog.asset`（右键 → Martian > Audio > Audio Catalog）
2. 同目录创建 `AudioSettings.asset`（右键 → Martian > Audio > Audio Settings）
3. AudioSettings 配置：Pool Size = 16，Music Sources = 2，Fade = 0.5s
4. 主场景创建空 GameObject `[Audio]`，挂 `AudioBootstrap` + `AudioEventBridge`，引用两个资产

---

## Cue ID 列表

| cue id | Bus | pitchMin/Max | cooldown | 说明 |
|---|---|---|---|---|
| bgm.main | Music | 1 / 1 | 0 | 主循环 BGM |
| bgm.result | Music | 1 / 1 | 0 | 结算/游戏结束 BGM |
| sfx.card.play | Sfx | 0.95 / 1.05 | 0 | 出牌 |
| sfx.coin.gain | Sfx | 1 / 1 | 0.05 | 获得金币（含还贷正向） |
| sfx.coin.lose | Sfx | 1 / 1 | 0.05 | 扣除金币/还贷 |
| sfx.turn.start | Sfx | 1 / 1 | 0 | 回合开始 |
| sfx.reward.show | Sfx | 1 / 1 | 0 | 奖励面板展开 |
| sfx.reward.pick | Sfx | 1 / 1 | 0 | 选择奖励 |
| ui.button | Ui | 1 / 1 | 0 | 通用按钮点击 |

> Clips 留空，有音频资产后填入 AudioCatalog 对应条目的 `clips` 数组即可，代码不用动。

---

## AudioEventBridge 事件映射

| GameEvent | 播放 |
|---|---|
| `GameEvents.CardPlayed` | sfx.card.play |
| `GameEvents.TurnStarted` | sfx.turn.start |
| `GameEvents.CardRewardOffered` | sfx.reward.show |
| `GameEvents.CardRewardSelected` | sfx.reward.pick |
| `GameEvents.MoneyChanged`（Delta > 0） | sfx.coin.gain |
| `GameEvents.MoneyChanged`（Delta ≤ 0） | sfx.coin.lose |
| `GameEvents.GameOver` | PlayMusic("bgm.result") |
| OnEnable（系统启动） | PlayMusic("bgm.main") |

---

## 音效触发点（代码位置）

### 直接调用 AudioServices

| 文件 | 行号 | 方法 | 音效 ID |
|------|------|------|---------|
| `UI/AudioOnClick.cs` | 35 | `OnClick()` | `ui.button`（Inspector 可配置） |
| `Integration/Martian/Audio/AudioEventBridge.cs` | 58 | `Start()` | `bgm.main` |
| `Integration/Martian/Audio/AudioEventBridge.cs` | 61 | `OnCardPlayed()` | `sfx.card.play` |
| `Integration/Martian/Audio/AudioEventBridge.cs` | 62 | `OnTurnStarted()` | `sfx.turn.start` |
| `Integration/Martian/Audio/AudioEventBridge.cs` | 63 | `OnRewardOffered()` | `sfx.reward.show` |
| `Integration/Martian/Audio/AudioEventBridge.cs` | 64 | `OnRewardSelected()` | `sfx.reward.pick` |
| `Integration/Martian/Audio/AudioEventBridge.cs` | 67 | `OnMoneyChanged()` | `sfx.coin.gain` / `sfx.coin.lose` |
| `Integration/Martian/Audio/AudioEventBridge.cs` | 70 | `OnGameOver()` | `bgm.result` |

### EventBus 事件发布点（触发上表订阅）

| 事件 | 发布文件 | 行号 | 方法 |
|------|---------|------|------|
| `CardPlayed` | `GameFlow/TurnManager.cs` | 804 | `ResolveCardAfterPlay()` |
| `TurnStarted` | `GameFlow/TurnManager.cs` | 128 | `StartTurn()` |
| `CardRewardOffered` | `GameFlow/TurnManager.cs` | 1220 | `AwardOneCardFromThreeOptions()` |
| `CardRewardSelected` | `UI/UICardRewardPanel.cs` | 123, 129 | `OnCardSelected()` / `OnCardDeselected()` |
| `MoneyChanged` | `Economy/MoneyManager.cs` | 72, 125 | `AddMoney()` / `ReduceMoney()` |
| `GameOver` | `GameFlow/TurnManager.cs` | 697 | `ProcessLoanPayment()` |

### Feedback 系统触发点（BaoZuPoFeedbackAdapter）

Feedback 系统不直接调用 AudioServices，由 Martian.Feedback 内部根据 FeedbackCategory 决定音效表现。

| 方法 | 调用来源 | 行号 | FeedbackCategory | 触发时机 |
|------|---------|------|-----------------|---------|
| `PublishSettlementSequence` | `UI/Settlement/UISettlementSequenceController.cs` | 251 | 结算序列 | 每一步结算浮字播放时 |
| `PublishSettlementMoneyJump` | `UI/Settlement/UISettlementSequenceController.cs` | 450 | 结算总额 | 整批结算总金额变化 |
| `PublishPlayCost` | `GameFlow/TurnManager.cs` | 805 | `Cost` | 出牌扣费 |
| `PublishInstantMoneyDelta` | `GameFlow/TurnManager.cs` | 817 | `Cost` / `Money` | 卡牌即时收益或消费 |
| `PublishLoanPayment` | `GameFlow/TurnManager.cs` | 706 | `Loan` | 结算阶段扣贷款 |

> 游戏内无 Unity 原生 `AudioSource.PlayOneShot` 调用，全部走 Martian.Audio 或 Martian.Feedback。

---

## 音量分组

| Bus | 控制范围 | PlayerPrefs 持久化 |
|---|---|---|
| Master | 所有声音 | ✅ |
| Music | BGM | ✅ |
| Sfx | 游戏音效 | ✅ |
| Ui | 按钮/UI 音效 | ✅ |

> 音量设置 UI 是独立任务，底层持久化已由包处理。

---

## 接入 Wwise（备忘）

1. 实现 `IWwiseAudioBackendFactory`，调用 `AudioBackendRegistry.Register(...)`
2. AudioSettings 中 `preferredBackendId` 改为 `"wwise"`
3. `AudioEventBridge`、`AudioOnClick`、业务代码**零改动**

