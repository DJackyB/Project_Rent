# UI系统

## 模块职责

- 负责手牌、棋盘、合同区、HUD、阶段面板、结算表现和奖励面板等主要界面显示。
- 负责拖拽出牌、Tooltip 预览、结算跳字反馈和奖励三选一交互。
- 负责根据 `GameEvents` 刷新玩家可见内容。
- 当前固定文案真源已经收口到 [GameText.cs](../../Assets/_Assets/Scripts/UI/GameText.cs)。

## 关键入口脚本

- [UIManager.cs](../../Assets/_Assets/Scripts/UI/UIManager.cs)
- [UITopBar.cs](../../Assets/_Assets/Scripts/UI/UITopBar.cs)
- [UIHandPanel.cs](../../Assets/_Assets/Scripts/UI/UIHandPanel.cs)
- [UIBoardPanel.cs](../../Assets/_Assets/Scripts/UI/UIBoardPanel.cs)
- [UIRoomView.cs](../../Assets/_Assets/Scripts/UI/UIRoomView.cs)
- [UIRoomSlotView.cs](../../Assets/_Assets/Scripts/UI/UIRoomSlotView.cs)
- [UICardView.cs](../../Assets/_Assets/Scripts/UI/UICardView.cs)
- [UICardRewardPanel.cs](../../Assets/_Assets/Scripts/UI/UICardRewardPanel.cs)
- [UICardDragController.cs](../../Assets/_Assets/Scripts/UI/UICardDragController.cs)
- [UICardDropZone.cs](../../Assets/_Assets/Scripts/UI/Common/Drag/UICardDropZone.cs)
- [GameText.cs](../../Assets/_Assets/Scripts/UI/GameText.cs)
- [TooltipServices.cs](../../Assets/_Assets/Martian/Tooltip/TooltipServices.cs)
- [TooltipRuntimeService.cs](../../Assets/_Assets/Martian/Tooltip/Runtime/TooltipRuntimeService.cs)
- [BaoZuPoCardTooltipPresenter.cs](../../Assets/_Assets/Scripts/Integration/Martian/Tooltip/BaoZuPoCardTooltipPresenter.cs)
- [BaoZuPoMartianTooltipIntegration.cs](../../Assets/_Assets/Scripts/Integration/Martian/Tooltip/BaoZuPoMartianTooltipIntegration.cs)
- [UIFeedbackPopupLayer.cs](../../Assets/_Assets/Scripts/UI/Common/FeedbackPopup/UIFeedbackPopupLayer.cs)
- [UIFeedbackPopupView.cs](../../Assets/_Assets/Scripts/UI/Common/FeedbackPopup/UIFeedbackPopupView.cs)
- [BaoZuPoFeedbackAdapter.cs](../../Assets/_Assets/Scripts/Integration/Martian/Feedback/BaoZuPo/BaoZuPoFeedbackAdapter.cs)
- [UISettlementSequenceController.cs](../../Assets/_Assets/Scripts/UI/Settlement/UISettlementSequenceController.cs)
- [UI手动配置与验收.md](./UI手动配置与验收.md)

## 当前实现

### UIManager 主链路

`UIManager` 当前监听：

- `PhaseChanged`
- `CardPlayed`
- `TurnStarted`
- `GameOver`
- `CardRewardOffered`

它负责：

- 刷新 HUD、手牌和棋盘
- 初始化拖拽控制器
- 安装 Tooltip 接线
- 同步资金反馈开关配置
- 收到奖励事件后显示 `UICardRewardPanel`

### 手牌与棋盘

- `UIHandPanel` 根据 `DeckManager.Hand` 实例化手牌 UI。
- `UIBoardPanel` 根据 `BoardManager` 刷新房间、合同和场上牌。
- `UIRoomView / UIRoomSlotView` 根据 `RoomSlot` 容量动态生成格位。
- `UICardView` 负责玩家可见卡牌信息与交互状态。
- 手牌上下文中，`UICardView` 显示名称、费用、描述、类型标签、等待、租金和耐久等完整信息；租金只在租客卡且 `baseRent > 0` 时显示，耐久只在当前耐久 `> 1` 时显示。

### 拖拽出牌

1. `UICardDragHandler` 负责手牌卡面的拖拽输入。
2. `UICardDragController` 接管真正的拖拽对象。
3. `UICardDropZone` 根据卡牌目标类型决定高亮与接收。
4. 松手时先调用 `TurnManager.ValidatePlay(...)`。
5. 合法则播放飞牌并执行 `TurnManager.PlayCard(...)`。
6. 非法则抖动并回弹。

### 结算表现与反馈

- `UISettlementSequenceController` 接收 `UISettlementPlaybackBatch`。
- 结算 step 和最终入账固定走 `UIFeedbackPopupLayer` / `UIFeedbackPopupView`。
- `BaoZuPoFeedbackAdapter` 负责把出牌扣款、即时资金变化、贷款扣款映射成 popup 请求。
- popup 位置由 `Anchor + ScreenOffset` 决定；钱栏相关 popup 通过 `UITopBar.MoneyTargetAnchor` 定位。
- `UITopBar.PlayCostPopupVerticalGap` 调整出牌/贷款扣款高度，`UITopBar.SettlementTotalPopupVerticalGap` 调整最终入账高度。
- `UITopBar` 默认尊重场景中已绑定文本的父物体和布局，只有运行时补齐文本时才应用默认布局。

### Tooltip

Tooltip 当前是“通用模块本体 + 可选运行时模块 + 项目接线层”的结构：

- 模块本体位于 `Assets/_Assets/Martian/Tooltip/`
- 运行时模块位于 `Assets/_Assets/Martian/Tooltip/Runtime/`
- 项目 presenter 位于 [BaoZuPoCardTooltipPresenter.cs](../../Assets/_Assets/Scripts/Integration/Martian/Tooltip/BaoZuPoCardTooltipPresenter.cs)
- 项目安装入口位于 [BaoZuPoMartianTooltipIntegration.cs](../../Assets/_Assets/Scripts/Integration/Martian/Tooltip/BaoZuPoMartianTooltipIntegration.cs)

运行时模块缺失时，会通过 `NullTooltipService` 安静降级，只影响显示，不影响流程。

### 奖励三选一

- `TurnManager` 发布 `CardRewardOffered`
- `UIManager.OnCardRewardOffered(...)` 调用 `UICardRewardPanel.Show(...)`
- `UICardRewardPanel` 生成最多 3 张不重复候选卡
- 玩家点击卡牌或“Skip”后，面板发布 `CardRewardSelected`
- `TurnManager` 收到结果后决定是否把奖励卡加入手牌并结束本回合

这条链路不是可选模块，而是主流程的一部分。

### 当前文案真源

当前主干实际运行的是：

- `GameText`
- 英文单真源
- 无运行时语言状态
- 无语言切换
- 无运行时字体切换链路

仓库中虽然已导入 `Martian.Localization` 和 `com.unity.localization`，但当前 UI 主链路没有接到那套多语言模块。

## 与其他模块的依赖

- 回合系统：通过 `GameEvents` 接收阶段变化、回合变化、结算播放完成和奖励事件。
- 棋盘系统：棋盘与房间 UI 直接读取 `BoardManager`、`RoomSlot`。
- 牌库系统：手牌区直接读取 `DeckManager`。
- 经济系统：顶栏金额与统计依赖 `MoneyManager`。

## 当前限制与风险

- `UICardRewardPanel` 属于主链路必需引用；场景或 prefab 没配好时会直接报错暴露问题。
- 奖励池唯一候选不足 3 张时，会 warning 并退化为更少选项。
- Tooltip 可降级，但结算/资金 popup 已是 UI 主链路的一部分；奖励面板不能按同样方式静默降级。
- `Martian.Localization` 当前只是已导入未接线模块，不应误认为当前运行时真源。

## 修改准则

- UI 不承接玩法规则判断，规则仍由 `TurnManager`、`CardTargeting` 和底层系统负责。
- 新增玩家可见固定文案时，优先接入 [GameText.cs](../../Assets/_Assets/Scripts/UI/GameText.cs)。
- 不要重新引入 `LanguageChanged`、`UseChinese`、运行时字体切换或同类旧本地化状态。
- Tooltip 的可选化边界不要反向污染主流程 UI；资金 popup 通用层不要直接依赖 Feel 或业务语义。
- 新增 UI 模块时，要同步补 [UI手动配置与验收.md](./UI手动配置与验收.md)。

## 手工验收点

- 开局后手牌、房间、合同区、顶栏和阶段面板是否都能正常显示。
- 租客、设备、事件牌拖到合法区域是否成功。
- 非法拖放是否能正确回弹。
- 结算跳字是否按顺序播放。
- 出牌扣款、贷款扣款和结算最终入账是否出现在 `Money Target Anchor` 上方。
- 结算后奖励面板是否能弹出。
- 奖励选择与跳过是否都能推进到下一回合。
- 移除 Tooltip 运行时模块后，是否只失去 Tooltip 显示，而不是阻断主流程。
